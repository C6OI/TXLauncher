namespace TXLauncher {
    enum ConnectionState {
        DISCONNECTED,
        WAITING_FOR_DATA,
        CONNECTED_TO_SERVER,
        READY,
        CLOSED_BY_CLIENT,
        CLOSED_BY_SERVER
    }
}
